K Smart Luxury — React + Vite

- Dev: `npm run dev`
- Build: `npm run build`
- Preview: `npm run preview`

Assets
- Place brand logo at `public/img/brand-logo.png` (or .jpg). It will show in the header automatically.
- Gallery images live in `public/img/`.

Deploy (Netlify)
- Set build command to `npm run build`
- Publish directory: `dist`